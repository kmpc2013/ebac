services:
  api:
    build: .
    environment:
      - DATABASE_URL=sqlite:///./livros.db
      - MEU_USUARIO=admin
      - MINHA_SENHA=admin
    container_name: api-livros
    ports:
      - "8000:8000"
    volumes:
      - livros_data:/app
    restart: unless-stopped

volumes:
  livros_data:
